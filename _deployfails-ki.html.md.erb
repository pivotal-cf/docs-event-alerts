+ **Deploy <%= vars.product_name %> Errand Fails:** If you have binary logging enabled,
the **Deploy <%= vars.product_name %>** errand fails when `log_bin_trust_function_creators` is not set to `1`.
If you do not resolve this issue, <%= vars.product_name %> is not deployed.
For instructions on solving this issue,
see [Deploy <%= vars.product_name %> Errand Fails](./troubleshooting.html#t-shoot-errors).
